import static org.junit.Assert.*;
import org.junit.Test;

public class ExplorerSearchTest {
    @Test
    public void testReachableArea_someUnreachable() {
        int[][] island = {
            {1,1,1,3,1,1},
            {3,2,3,1,3,1},
            {1,1,1,1,3,3},
            {3,1,2,1,0,1},
            {1,1,1,2,1,1},
        };
        int actual = ExplorerSearch.reachableArea(island);
        assertEquals(14, actual);
    }

    // findStart tests

    @Test 
    public void testFindStart_oneValidStart() {
        int[][] island = {
            {1,1,1,3,1,1},
            {3,2,3,1,3,1},
            {1,1,1,1,3,3},
            {3,1,2,1,0,1},
            {1,1,1,2,1,1},
        };

        int[] actual = ExplorerSearch.findStart(island);
        int[] expected = {3, 4};

        assertArrayEquals(expected, actual);
    }

    // test throws exception for no start
    // test multiple starts
    // test first is start / last is start

    // Add more tests here!
    // Come up with varied cases
}
